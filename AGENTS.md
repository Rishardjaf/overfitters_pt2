# Agent instructions

1. Read [agent_knowledge/README.md](agent_knowledge/README.md), then [INDEX.md](agent_knowledge/INDEX.md), and open only relevant topic files. Check [open conflicts](agent_knowledge/conflicts/OPEN.md).
2. Treat [the revised release](agent_knowledge/findings/dataset_release.md) as the current input contract. `current_PM2_5` is absent. Do not restore it from old data, cached features, shifted target labels, or external sources without an explicitly approved inference-time data contract.
3. Read [the migration review](agent_knowledge/findings/previous_knowledge_review.md) before reusing anything from the previous repository. Historical scores and conclusions are not validated on this release.
4. Keep `data/raw/` immutable. Preserve IDs and timestamps. Use only information available by the observation hour; learn preprocessing from training data. Follow [the validation policy](agent_knowledge/decisions/causal_validation.md).
5. Record reusable findings with dataset fingerprints, scope, evidence, confidence, and date. Use the templates, update the index, and preserve contradictory findings through the conflict workflow. Mark untested features and models as proposed.
6. Follow the folder layout in [README.md](README.md). This migration is intentionally an empty scaffold outside agent knowledge and raw data; add implementations only when a subsequent task requests them.
7. Before Git work, read applicable personal Git safety rules (on the original workstation: `C:\Users\risha\.codex\GIT_RULES.md`), inspect status, branch/upstream, remotes, and worktrees, and fetch before integrating. Preserve user changes. Work on `type/change_name` branches using underscores; do not use `codex` or `codex/`. Use Conventional Commits and explicit staging. Before pushing, dry-run the same explicit remote/refspec and verify its destination. Do not push to `main` or `master` without explicit authorization for that destination. Use draft PRs when a base branch exists; run relevant validation before merging.
